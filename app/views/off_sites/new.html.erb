<button class="btn btn-light" id="backButton"><i class="fa fa-arrow-left" aria-hidden="true"></i></button>
<h1 align="center">New Off Site Sheet</h1>

<%= form_for :off_site, url: { action: "create" } do |f| %>
  <div align="right">
    <%= f.submit "Create Off Site Sheet", class: "btn btn-success" %>
  </div>
  <%= render partial: 'form', locals: { f: f } %>
  <div align="right">
    <%= f.submit "Create Off Site Sheet", class: "btn btn-success" %>
  </div>
<% end %>
<div class="Footer-Space">

</div>
