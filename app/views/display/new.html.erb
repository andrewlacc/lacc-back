<div class="pad">
  <%= form_for @display, url: { action: "create", params: { id: @display.id } } do |f| %>
    <%= link_to '<< Back', display_index_path, class: "btn btn-secondary" %>
    <h1>New Refurbished Display Input Tag</h1>
    <%= render partial: 'form', locals: { f: f } %>
    <div>
      <%= f.submit "Create Tag", class: "btn btn-success" %>
    </div>
  <% end %>
</div>
